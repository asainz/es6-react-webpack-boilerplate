# es6-react-webpack-boilerplate

# Run the app in the browser

```
$ npm run work
```

# Build the app

```
$ npm run build
```


npm run work --env=qa
npm run work --env=local

npm run build --env=qa